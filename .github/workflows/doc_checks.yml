name: docs-checks

on: [push, pull_request]

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run doc checks script
        run: |
          chmod +x scripts/doc_checks.sh
          ./scripts/doc_checks.sh

This directory archives deprecated infrastructure files.

docker-compose.yml has been archived because Vikunja services were merged into docker-compose.yml.
Use docker-compose.yml for deployment; contact the repo owners for restore instructions.
